# Deployment

The portfolio is a static site. Deploy `index.html` with GitHub Pages, Vercel, Netlify, or any static host.

The active rebuild is maintained on `devhq-rebuild` until merged into `main`.
